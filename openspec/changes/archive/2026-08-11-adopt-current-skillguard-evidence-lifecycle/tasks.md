## 1. Freeze Current Authorities

- [x] 1.1 Audit coordination, Git identity, current OpenSpec artifacts, target `SKILL.md`, eleven native checks, consumer projection, and existing evidence roots.
- [ ] 1.2 Upgrade the FlowGuard project record to the installed current engine only after the global consumer authority is current, then run affected lifecycle/model checks. **Blocked:** the separate FlowGuard source/consumer worktree is still dirty and its installed package-authority suite reports `installed_raw_hash_mismatch` and `consumer_release_identity_mismatch`; this repository does not overwrite or bypass that parallel work.
- [x] 1.3 Adopt the current SkillGuard `0.7.2` author contract directly, preserving the target-owned route and exact declared checks.

## 2. Compile and Validate

- [x] 2.1 Regenerate the SkillGuard compiled contract and check manifest from the complete current target inventory.
- [x] 2.2 Freeze exact check owners, evidence subjects, dependencies, inputs, toolchain, environment, and private receipt root.
- [x] 2.3 Run affected checks during implementation and inspect every failure, skip, stale result, and claim boundary.
- [ ] 2.4 Freeze the post-archive release snapshot and run exactly one final full validation owner for all eleven target-declared checks.

## 3. Bound Evidence and Installation

- [x] 3.1 Create current durable terminal evidence and an exact lifecycle audit/plan for historical receipts, run roots, staging data, logs, and caches. (`docs/skillguard-evidence-lifecycle-3.0.1.md` records the boundary and the post-archive receipt location.)
- [x] 3.2 Quarantine or remove only planned non-authoritative material after reference and process checks; verify source and protected evidence remain intact. (Audit found no safe, unreferenced cleanup target; no destructive deletion or blind quarantine was performed.)
- [x] 3.3 Build, content-verify, activate, and audit a clean consumer projection without author-control or cache files.
- [x] 3.4 Refresh the private global router only from explicit author roots if the Logic Writing route projection changed; do not add an updater. (The route projection did not change; installed route parity was checked and no router write was needed.)

## 4. Release and Closure

- [x] 4.1 Update version `3.0.1`, changelog, README/release presentation, privacy boundary, and consumer release metadata for the current snapshot.
- [ ] 4.2 Verify source, generated contract, native receipt, installation, package, Git, tag, and GitHub Release identities independently.
- [ ] 4.3 Complete OpenSpec/FlowGuard closure records and archive both active changes after the post-archive source is validated.
- [ ] 4.4 Commit and push only owned files, create `v3.0.1`, publish a source-only GitHub Release, and perform the predictive-KB postflight.
