interface:
  display_name: "Logic Writing"
  short_description: "Plan the whole artifact, write coherent prose, and verify the real result"
  default_prompt: "Use $logic-writing to preserve the complete ReaderIntent, select one final owner, build a whole-artifact CompositionPlan, draft one integrated reader-facing artifact, bind and review its actual current spans, repair real defects, and close only on current evidence."
